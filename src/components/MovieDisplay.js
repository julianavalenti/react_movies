
// Define a function Component, always make sure to declare the props parameter so you can use props in your Component
const MovieDisplay = (props) => {
    // TheComponent must return some JSX
    return <h1>The MovieDisplay Component</h1>;
  };
  
  // We must export the Component to use it in other files
  export default MovieDisplay;